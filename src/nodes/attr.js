// ;(function (root, Structure, undefined){
//   'use strict';

//   var Attr = function (name) {
//     this.initialize.call(this, Structure.Node.ATTR, name);
//   };

//   Attr.prototype = copy(Structure.Node.prototype);
//   Attr.prototype.constructor = Attr;

//   // root.Structure || (root.Structure = {});
//   root.Structure.Attr = Attr;

// }(this, this.Structure));
